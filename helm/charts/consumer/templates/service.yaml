apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.bbconsumer.name }}
  labels:
    group: {{ .Values.bbconsumer.group }}
spec:
  type: {{ .Values.bbconsumer.service.type }}
  selector:
    app: {{ .Values.bbconsumer.name }}
  ports:
    - port: {{ .Values.bbconsumer.service.port }}
      targetPort: {{ .Values.bbconsumer.container.port }}
      {{ if eq .Values.bbconsumer.service.type "NodePort" }}nodePort: {{ .Values.bbconsumer.service.nodeport }}{{ end }}