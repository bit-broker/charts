apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  namespace: '{{ .Release.Namespace }}'
  labels:
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  JWKS: {{ .Values.JWKS }}
